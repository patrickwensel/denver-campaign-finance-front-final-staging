import { version } from '../../package.json';

export const environment = {
  production: false,
  snackBarDuration: 3000,
  version:version,
  baseUrl: 'https://denverapi-stg.augustasoftsol.com/', // End point needed
};
