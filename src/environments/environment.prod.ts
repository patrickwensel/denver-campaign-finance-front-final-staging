import { version } from '../../package.json';

export const environment = {
  production: true,
  snackBarDuration: 3000,
  environment:'production',
  version:version,
  baseUrl: 'https://denverapi-qa.augustasoftsol.com/',
};
